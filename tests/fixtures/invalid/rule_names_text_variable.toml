#:schema ../../../schemas/v0/ryspec.schema.json
version = 0

# Rule 9: a rule naming a text or binary variable.

[variables]
frame = { type = "text", format = "json" }

[[properties]]
name = "p"
check = "frame"
