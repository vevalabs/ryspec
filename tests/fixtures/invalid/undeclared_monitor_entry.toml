#:schema ../../../schemas/v0/ryspec.schema.json
version = 0

# Rule 3: an inputs or parameters entry with no [variables] declaration.
# outputs is the exception -- see unresolved_output.toml.

[monitor]
inputs = ["x", "y"]
outputs = ["p"]

[variables]
x = {}

[[properties]]
name = "p"
check = ["gt", "x", 0]
